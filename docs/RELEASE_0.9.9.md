# Flex 0.9.9 / 909

Baseline: registered 0.9.8 source 25fe7c1735314f64a41e184c494b9bfb396a630a,
plus source-only report work 47ca141f37fe9f553f6d2f39d99e95ebafc9ee71.
Version generated by the unchanged npm run version:next in authenticated CI run
36097510329, then applied to both package files. Live registry rechecked before publication.

- Adds Lisää → Kyselyt ja arvot with a copyable report of all current-profile
  live definitions, actual values, missing values, connection stages and traffic.
- Keeps earlier valid support bitmap pages if a later optional page fails.
- Enables existing Toyota discovery for generic ELM readers, with exact existing
  raw fallback for failed IS220d live queries and atomic adapter restoration.
- Offers explicit fuel temperature, Toyota rail pressure and injection timing
  comparison reads. They are not promoted to vehicle-verified Live measurements.
- Keeps the rejected injector query blocked and Quicklynks binary transport separate.

No new query identifiers, conversion formulas, Android permissions or ECU writes.
Deterministic tests cover fallback, queue ordering, failure restoration, null data,
profile boundaries and comparison isolation. APK UI smoke exercises the bundled report.

Field validation: connect ELM, run Live and reproduce missing values, then open
Lisää → Kyselyt ja arvot and generate/copy the report. Stop Live before the optional
comparison read and copy again. Compare those readings against Techstream.
Previous field NO DATA replies, including raw requests, are not proven fixed by
simulations. Real-vehicle availability and EGR semantics still need field evidence.
